<div class="container ">
  <div class="row mb-3">
    <h3>会員一覧</h3>
  </div>

  <table class="table  table-hover">
      <thead>
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>会社名</th>
        <th>ステータス</th>
      </tead>
      <% @customers.each do |customer| %>
        <tr>
          <td><%= customer.id %></td>
          <td><%= link_to (customer.name), admin_contractor_path(customer.id) %></td>
          <td><%= customer.email %></td>
          <td><%= customer.company_name %></td>
          <td>
            <% if customer.is_menber == true %>
              <b class="text-muted">退会</b>
            <% else %>
              <b class="text-success">有効</b>
            <% end %>
          </td>
        </tr>
      <% end %>
  </table>
</div>
